import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
  return (
    <Base title="UserDashboard">
      <h1>User's UserDashboard Page</h1>
    </Base>
  );
};

export default UserDashboard;
